# Guix System or NixOS system test

I plan to replace LEAP 16 with Xfce/X11 with something else, because SUSE now intentionally cripples, breaks
packages (I alredy use Guix package manager to replace broken/missing `audacious`, `keepass`, `wine`, `virt-viewer`...)

I'm unable to decide, what is better, so I decided to spin 2 VMs

1st VM Guix System (formerly known as GuixSD) with Xfce
2nd VM NixOS System with Xfce


Let's start!

# After installation

Problem: NixOS had not enable sshd (Guix installer offered that as option). See later...




